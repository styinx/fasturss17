Spigo:

    Architektur als Eingabe (json):
        - victim
        - services
            - name, package, regions, count, dependencies

    Ausgabe:

        Graph (neo4j):
            - gerichteter Graph            

        Metriken:
            - Zahlensalat
